# Production Environment Variables for Vercel
# Add these to Vercel Dashboard: Settings -> Environment Variables

# MongoDB Atlas (REQUIRED - Get from MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/eco_bottle?retryWrites=true&w=majority

# JWT Secrets (REQUIRED - Generate secure random strings)
JWT_SECRET=your-production-secret-key-change-this
JWT_REFRESH_SECRET=your-production-refresh-secret-change-this

# API Configuration
NEXT_PUBLIC_API_URL=/api

# Optional: Redis (Use Upstash for serverless Redis)
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Optional: Analytics
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
NEXT_PUBLIC_GA_ID=